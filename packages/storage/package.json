{
  "name": "@contextweft/storage",
  "version": "0.0.0-development",
  "description": "Canonical SQLite storage for ContextWeft workspaces, events, and artifacts.",
  "type": "module",
  "license": "Apache-2.0",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "bench": "tsx bench/storage.bench.ts",
    "build": "rm -rf dist && tsup src/index.ts --format esm --sourcemap --external better-sqlite3 && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@contextweft/contracts": "workspace:*",
    "better-sqlite3": "13.0.3"
  }
}
